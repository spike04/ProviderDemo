import 'package:flutter/material.dart';

class MultiProviderSample extends StatelessWidget {
  MultiProviderSample({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Multi Provider Sample'),
      ),
      body: Center(
        child: Text('MultiProviderSample'),
      ),
    );
  }
}
